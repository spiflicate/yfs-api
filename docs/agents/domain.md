# Domain Docs

This is a single-context repository.

Before exploring a domain change:

- Read `docs/CONTEXT.md`.
- Check `docs/adr/` for applicable decisions when that directory exists.
- Use the glossary vocabulary in issue titles, plans, tests, and implementation notes.

Missing ADR or context files should not block work. Create them only when a domain decision needs to be recorded.
