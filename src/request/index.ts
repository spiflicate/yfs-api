/**
 * Request Builder Module
 *
 * A type-safe, composable request builder for the Yahoo Fantasy API.
 *
 * @module
 */

export {
   createRequest,
   RequestBuilder,
   type RootRequestBuilder,
} from './builder.js';
